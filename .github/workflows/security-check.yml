name: Public Repository Safety Check

on:
  workflow_dispatch:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  public-safety:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Reject tracked secret files
        shell: bash
        run: |
          set -euo pipefail
          bad_files="$(git ls-files | grep -E '(^|/)(\.env($|\.)|.*\.(pem|key|p12|pfx)$)' || true)"
          if [ -n "$bad_files" ]; then
            echo "Potential secret files are tracked:"
            echo "$bad_files"
            exit 1
          fi

      - name: Scan repository for common credential patterns
        shell: bash
        run: |
          set -euo pipefail
          patterns='(ghp_[A-Za-z0-9]{30,}|github_pat_[A-Za-z0-9_]{30,}|gh[pousr]_[A-Za-z0-9]{20,}|AKIA[0-9A-Z]{16}|-----BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY-----|sk-[A-Za-z0-9_-]{20,})'
          if git grep -nEI "$patterns" -- . ':!*.png' ':!*.jpg' ':!*.jpeg' ':!*.webp' ':!*.ico'; then
            echo "Potential credential detected in public repository."
            exit 1
          fi
          echo "No common credential patterns detected."

      - name: Reject private portfolio fields from public data
        shell: bash
        run: |
          set -euo pipefail
          private_fields='"(shares|quantity|position_size|average_cost|cost_basis|purchase_price|account_type|position_id|planned_total_shares|stage_size|completed_stages|max_stages|opened_at|closed_at|brokerage_account|brokerage_account_id|portfolio_id)"[[:space:]]*:'
          if grep -R -nEI "$private_fields" docs/data; then
            echo "Private portfolio/account field detected in public data."
            exit 1
          fi
          echo "No private portfolio/account fields detected in public data."

      - name: Reject private burn-in evidence from public repository
        shell: bash
        run: |
          set -euo pipefail
          if git ls-files | grep -Eq '(^|/)burnin_evidence\.json; then
            echo "Private burn-in evidence must not be tracked in the public PWA repository."
            git ls-files | grep -E '(^|/)burnin_evidence\.json
            exit 1
          fi
          echo "No private burn-in evidence is tracked publicly."
